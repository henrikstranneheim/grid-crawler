#!/usr/bin/env perl

###Copyright 2016 Henrik Stranneheim

use TAP::Harness;

my %args = (verbosity => 1);

my $harness = TAP::Harness->new( \%args );
my @tests = [ 'test.t' ];
$harness->runtests(@tests);
